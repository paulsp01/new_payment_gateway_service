name: Node.js CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '14'  # Adjust node version as per your requirements

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test

    - name: Build Docker image
      run: |
        docker build -t myapp:${{ github.sha }} .
        echo "DOCKER_IMAGE=myapp:${{ github.sha }}" >> $GITHUB_ENV

    - name: Log in to Docker Hub
      env:
        DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: echo $DOCKER_PASSWORD | docker login --username $DOCKER_USERNAME --password-stdin

    - name: Push Docker image to Docker Hub
      env:
        DOCKER_IMAGE: ${{ env.DOCKER_IMAGE }}
      run: docker push $DOCKER_IMAGE

    - name: Deploy to Render.com
      env:
        RENDER_TOKEN: ${{ rnd_OoE6S09D2EGgqBTEKM2al6Ffdxrl }}
       
      run: |
        render deploy -- --branch main --build-env DOCKER_IMAGE=$DOCKER_IMAGE
